# Server-only upstream API origin. Do not expose this as NEXT_PUBLIC_*.
XESPORTS_API_URL=https://apis.xesports.pro

# Optional analytics. Google Tag Manager is loaded only after explicit consent.
NEXT_PUBLIC_GTM_ID=

# Optional public community link used by the landing-page CTA.
NEXT_PUBLIC_DISCORD_LINK=
